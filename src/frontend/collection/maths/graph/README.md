# Graph

Graph owns graph-specific composition helpers that are not purely calculus or data geometry.

Use this folder for components that help authors present plotted information: legends, graph keys,
series labels, and graph-specific annotation helpers. Continuous functions, fields, curves, and
surfaces live in `maths::calculus`; sampled point data lives in `maths::data_geometry`.
